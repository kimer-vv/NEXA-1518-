Replace ONLY the root file ui-i18n-final.js.
This version stops using a second Supabase/auth path for the constellation and instead uses the same account cache already loaded by index.html.
Expected: Kimer in center, Test as an orbit account, single tap = passport, double tap = switch active account.
