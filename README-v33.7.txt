NEXA v33.7 — Testing fixes

Key fixes:
- Manage SvS / Create / Edit route into the real NEXA Control Center in index.html.
- Clone Settings removed from SvS History.
- Archive view uses explicit event ID and safer DOM access.
- Transfer Application Review JavaScript syntax error fixed (Edit Applicant Details).
- Waiting List View Details uses waiting entry lookup reliably.
- Transfer public buttons stack cleanly on mobile; Apply is no longer hidden off-screen.
- Edit My Application link added directly to public Transfer Form.
- Transfer token SQL made more robust for new submissions.
- Alliance Distribution supports copying one alliance roster, while Copy by Alliance remains.
- Prep success message scrolls into view near submission feedback.
- Prep Form direct-link copy added to the real Admin SvS Control Center with iOS fallback.
- Coordinates helper clarifies X/Y format.

Run the appended v33.7 SQL section (or the full SQL file) before testing new Transfer Edit Tokens.
