NEXA • Phase 2A — Real Form Order + Safe Duplicate Fix

Replace:
- event-operations.html
- battle-form.html

battle-settings.html is included only to keep this ZIP aligned with the
latest approved Phase 2A files.

Fix 1 — Real Form Order
- Preview Battle Form and Open Form now use the same saved layout as
  Arrange & Preview.
- Saved Event Questions can be positioned between Combat Base fields,
  not just sorted among themselves.
- Layout remains separate by Event Type + Joiner/Rally.

Fix 2 — Duplicate Question
- Duplicate no longer adds "Copy" to the visible question text.
- A duplicate is created INACTIVE.
- It starts unassigned to a real Event Type, so it cannot appear in the
  original form accidentally.
- The duplicate opens for editing; choose Event Type/Role/etc. and
  activate it when ready.

No SQL required.
