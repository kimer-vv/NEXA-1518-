# NEXA v28.8 — Transfer Form Visible

Fix:
- The Transfer Form panel existed in v28.7, but the visible Transfer Form tab was missing from the Transfer Control Center row.
- Adds the `Transfer Form` tab next to Applications.
- Adds `View Transfer Form` shortcut inside Applications.
- Transfer Form tab now visibly lists the full question set you and I designed.
- Select a Transfer Event and use:
  - Open / Preview Form
  - Copy Direct Form Link
- The actual public form is `transfer-apply.html` and submissions go directly to Applications.

No SQL is required if v28.6 SQL already ran successfully.
