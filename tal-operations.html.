<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>NEXA • TAL Operations</title>
<!-- NEXA TAL OPERATIONS V1.0 • TAL SETUP • STATE-SCOPED SUPABASE STORAGE -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
:root{--bg:#02030b;--panel:#08101f;--line:rgba(255,255,255,.11);--text:#fff;--muted:#aeb7d1;--cyan:#48ddff;--violet:#9b78ff;--pink:#ff79d6;--green:#67dfb1;--gold:#ffc75b}
*{box-sizing:border-box}body{margin:0;min-height:100dvh;background:radial-gradient(circle at 5% 22%,rgba(44,194,255,.08),transparent 28%),radial-gradient(circle at 95% 8%,rgba(104,78,255,.10),transparent 32%),var(--bg);color:var(--text);font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,textarea{font:inherit}button{cursor:pointer}.sh{width:min(760px,100%);margin:auto;padding:26px 18px 110px}.top{display:grid;grid-template-columns:48px 1fr 48px;gap:12px;align-items:center}.round{width:48px;height:48px;border-radius:50%;border:1px solid rgba(72,221,255,.36);background:#081329;color:#bff6ff;display:grid;place-items:center;text-decoration:none;font-size:28px}.title{text-align:center;min-width:0}.title small{color:#aab4d1;font-size:10px;font-weight:950;letter-spacing:.2em}.title h1{margin:5px 0;font-size:clamp(2rem,8vw,3rem)}.state{text-align:center;color:#8f9bbb;font-size:11px;font-weight:800;margin:7px 0 0}.nav{display:flex;gap:8px;overflow-x:auto;margin:19px 0 16px;padding-bottom:4px;scrollbar-width:none;-webkit-overflow-scrolling:touch}.nav::-webkit-scrollbar{display:none}.nav button{flex:0 0 auto;min-height:44px;border-radius:22px;border:1px solid rgba(109,130,255,.35);background:#070b1f;color:#b8c5e4;padding:0 14px;font-weight:900}.nav button.active{color:#fff;border-color:rgba(148,111,255,.8);background:linear-gradient(135deg,rgba(75,54,170,.9),rgba(28,82,130,.9))}.nav button:disabled{opacity:.44;cursor:not-allowed}.card,.desc{border:1px solid var(--line);border-radius:20px;background:rgba(9,17,38,.82);padding:16px;margin-bottom:14px}.desc{color:var(--muted);line-height:1.5}.card h2,.card h3{margin:0 0 7px}.muted{color:var(--muted)}.tiny{font-size:12px;line-height:1.5}.mini{font-size:10px;letter-spacing:.12em;font-weight:950;text-transform:uppercase;color:#a7b4d4}.row{display:flex;align-items:center;gap:9px;flex-wrap:wrap}.between{justify-content:space-between}.grid2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.field{display:grid;gap:6px;color:#aeb7d1;font-size:12px;font-weight:850}input,textarea{width:100%;min-height:44px;background:#060b1c;color:#fff;border:1px solid rgba(122,142,203,.28);border-radius:12px;padding:10px}textarea{min-height:104px;resize:vertical;line-height:1.45}input:focus,textarea:focus{outline:2px solid rgba(72,221,255,.38);outline-offset:1px}.btn{min-height:44px;border-radius:13px;border:1px solid rgba(72,221,255,.36);background:linear-gradient(135deg,rgba(19,104,111,.72),rgba(32,75,118,.76) 54%,rgba(61,49,139,.76));color:#fff;font-weight:900;padding:9px 13px}.btn.violet{border-color:rgba(159,116,255,.5);background:linear-gradient(135deg,rgba(68,43,145,.86),rgba(47,69,138,.78))}.btn:disabled{opacity:.55;cursor:wait}.notice{border:1px solid rgba(72,221,255,.21);background:rgba(72,221,255,.045);border-radius:14px;padding:12px;color:#caefff}.status{min-height:20px;margin:0 0 12px;color:#82f0ff;font-size:12px;line-height:1.45}.status.error{color:#ff9db3}.hidden{display:none!important}.section{border-top:1px solid rgba(255,255,255,.09);padding-top:17px;margin-top:18px}.section h3{color:#ecedff}.hint{color:#98a7c9;font-size:11px;line-height:1.5;margin:4px 0 11px}.chip{display:inline-block;border:1px solid rgba(155,120,255,.38);color:#d9cbff;border-radius:99px;padding:5px 9px;font-size:10px;font-weight:950;letter-spacing:.06em}.summary-item{border:1px solid rgba(255,255,255,.09);border-radius:13px;background:rgba(255,255,255,.025);padding:10px;min-width:0}.summary-item small{display:block;color:#8f9bbb;font-size:9px;font-weight:950;letter-spacing:.09em;text-transform:uppercase}.summary-item strong{display:block;margin-top:5px;overflow-wrap:anywhere}.summary-notes{margin:12px 0 0}.summary-notes h3{font-size:13px;color:#c6b2ff;margin:0 0 5px}.summary-notes p{margin:0;white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px;line-height:1.5;color:#d8e2f4}.summary-notes article{padding:12px;border:1px solid rgba(255,255,255,.08);border-radius:12px;background:rgba(255,255,255,.02);margin-top:8px}.foot{text-align:center;color:#667794;font-size:10px;margin-top:18px}@media(max-width:620px){.sh{padding:20px 14px 96px}.grid2{grid-template-columns:1fr}.card,.desc{padding:14px}.title h1{font-size:clamp(1.7rem,8vw,2.4rem)}}
</style>
</head>
<body><main class="sh">
<div class="top"><a class="round" href="index.html" aria-label="Back to NEXA">‹</a><div class="title"><small>BATTLE OPERATIONS</small><h1>TAL</h1></div><a class="round" href="index.html" aria-label="Close">×</a></div>
<div class="state" id="stateLabel">Checking State…</div>
<nav class="nav" aria-label="TAL sections"><button class="active" type="button">TAL Setup</button><button type="button" disabled title="Upcoming">Formations</button><button type="button" disabled title="Upcoming">Responses</button><button type="button" disabled title="Upcoming">Legions</button><button type="button" disabled title="Upcoming">Team Roster</button><button type="button" disabled title="Upcoming">Battle Plan</button></nav>
<div class="desc">Set the TAL battle date, UTC time and strategy instructions. This setup is shared within the selected NEXA State; Legion 1 and Legion 2 will use the same event setup, with independent rosters and plans later.</div>
<div id="message" class="status" role="status" aria-live="polite">Connecting to NEXA…</div>
<section class="card" id="editor" hidden>
 <div class="row between"><div><span class="mini">EVENT SETUP</span><h2 style="margin-top:5px">TAL Setup</h2></div><span class="chip">UTC</span></div>
 <div class="grid2" style="margin-top:14px"><label class="field">Battle Date (UTC)<input id="battleDate" type="date" required></label><label class="field">Battle Start Time (UTC)<input id="battleTime" type="time" required step="60"></label></div>
 <label class="field" style="margin-top:12px">Event / Alliance Name (optional)<input id="eventName" type="text" maxlength="140" placeholder="TAL • State 1518"></label>
 <div class="section"><h3>Battle Timeline</h3><p class="hint">Offsets are minutes after battle start. Workshop spawn times are editable—nothing is assumed or locked.</p>
 <div class="grid2"><label class="field">Wave 2 Objectives Open (minute)<input id="phaseTwoMinute" type="number" min="0" max="120" step="1" placeholder="e.g. 15"></label><label class="field">Workshop Wave 1 (minute)<input id="workshopOneMinute" type="number" min="0" max="120" step="1" placeholder="Set when confirmed"></label><label class="field">Workshop Wave 2 (minute)<input id="workshopTwoMinute" type="number" min="0" max="120" step="1" placeholder="Set when confirmed"></label></div>
 </div>
 <div class="section"><h3>Wave Instructions</h3><p class="hint">These instructions will feed the final Battle Plan. Building and player assignments will be configured in Team Roster.</p>
 <label class="field">Wave 1 — Opening Facilities<textarea id="waveOne" placeholder="Opening objectives, positioning, communication and instructions…"></textarea></label>
 <label class="field" style="margin-top:12px">Wave 2 — Main Objectives<textarea id="waveTwo" placeholder="Imperial Foundry, Mercenary Camp, Munitions Warehouse and other objectives…"></textarea></label></div>
 <div class="section"><h3>Workshops</h3><p class="hint">Two Workshop Waves; four colored zones per wave (Top Left, Top Right, Bottom Left, Bottom Right). Players and the visual map will be inside Team Roster.</p>
 <label class="field">Workshop Wave 1 Instructions<textarea id="workshopOne" placeholder="First Workshop Wave strategy…"></textarea></label>
 <label class="field" style="margin-top:12px">Workshop Wave 2 Instructions<textarea id="workshopTwo" placeholder="Second Workshop Wave strategy…"></textarea></label></div>
 <div class="section"><h3>Loot Team</h3><p class="hint">Six positions will be assigned in Team Roster: Top Left, Top Right, Middle Left, Middle Right, Bottom Left and Bottom Right.</p><label class="field">Loot Collection Instructions<textarea id="loot" placeholder="Teleport, loot collection, explosions, rally response…"></textarea></label></div>
 <div class="section"><h3>Alternative Strategy</h3><p class="hint">Use this for an enemy-strength contingency or another strategy you may switch to during battle.</p><label class="field">If Enemy Is Strong / Alternative Strategy<textarea id="alternative" placeholder="When to adjust objectives, defend repairs and prioritize Workshops…"></textarea></label></div>
 <div class="row" style="margin-top:20px"><button class="btn violet" id="save" type="button">Save TAL Setup</button><button class="btn" id="cancel" type="button" hidden>Cancel Edit</button></div>
 <p class="hint" style="margin-bottom:0">Saving here does not publish a Live Event or a Battle Plan.</p>
</section>
<section class="card" id="summary" hidden>
 <div class="row between"><div><span class="mini">SAVED SETUP</span><h2 style="margin-top:5px">TAL Setup</h2></div><span class="chip">SAVED</span></div>
 <div class="grid2" style="margin-top:13px"><div class="summary-item"><small>Battle Date</small><strong id="sumDate">—</strong></div><div class="summary-item"><small>Battle Start</small><strong id="sumTime">—</strong></div><div class="summary-item"><small>Event</small><strong id="sumName">—</strong></div><div class="summary-item"><small>Wave 2 Opens</small><strong id="sumPhase">—</strong></div><div class="summary-item"><small>Workshop Wave 1</small><strong id="sumW1">—</strong></div><div class="summary-item"><small>Workshop Wave 2</small><strong id="sumW2">—</strong></div></div>
 <div id="summaryNotes" class="summary-notes"></div><div class="row" style="margin-top:15px"><button class="btn violet" id="edit" type="button">Edit Setup</button></div>
 <p class="hint" style="margin-bottom:0">Last saved: <span id="savedAt">—</span></p>
</section>
<div class="foot">NEXA TAL OPERATIONS V1.0 • TAL SETUP</div>
</main>
<script>
(()=>{'use strict';
const SB_URL='https://dfxcxboxrkfmrnsgpyin.supabase.co',SB_KEY='sb_publishable_HTd6T3L8WuN_owZwPUjE1Q_glB9YWM-';
const $=id=>document.getElementById(id);
const rawState=localStorage.getItem('nexa_active_state_v49')||window.NEXA_ACTIVE_STATE||'';
const state=Number(String(rawState).match(/\d+/)?.[0]||0);
const fields=['waveOne','waveTwo','workshopOne','workshopTwo','loot','alternative'];
const labels={waveOne:'Wave 1 — Opening Facilities',waveTwo:'Wave 2 — Main Objectives',workshopOne:'Workshop Wave 1',workshopTwo:'Workshop Wave 2',loot:'Loot Team',alternative:'Alternative Strategy'};
let sb,original=null,busy=false,editing=true,dirty=false;
const txt=(v)=>String(v??'');
function status(msg,error=false){$('message').textContent=msg;$('message').classList.toggle('error',error)}
function show(editor){editing=editor;$('editor').hidden=!editor;$('summary').hidden=editor}
function numberOrNull(id){const v=$(id).value.trim();if(!v)return null;const n=Number(v);if(!Number.isInteger(n)||n<0||n>120)throw Error('Timeline minutes must be whole numbers between 0 and 120.');return n}
function fill(row){const t=row?.timeline||{},i=row?.instructions||{};$('battleDate').value=row?.battle_date||'';$('battleTime').value=txt(row?.battle_time_utc).slice(0,5);$('eventName').value=row?.event_name||'';$('phaseTwoMinute').value=t.phaseTwoMinute??'15';$('workshopOneMinute').value=t.workshopOneMinute??'';$('workshopTwoMinute').value=t.workshopTwoMinute??'';fields.forEach(k=>$(k).value=i[k]||'');dirty=false}
function collect(){const date=$('battleDate').value,time=$('battleTime').value;if(!/^\d{4}-\d{2}-\d{2}$/.test(date))throw Error('Select a TAL Battle Date.');if(!/^([01]\d|2[0-3]):[0-5]\d$/.test(time))throw Error('Select a valid UTC Battle Start Time.');const instructions={};fields.forEach(k=>instructions[k]=$(k).value.trim());return{state_number:state,event_name:$('eventName').value.trim(),battle_date:date,battle_time_utc:time+':00',timeline:{phaseTwoMinute:numberOrNull('phaseTwoMinute'),workshopOneMinute:numberOrNull('workshopOneMinute'),workshopTwoMinute:numberOrNull('workshopTwoMinute')},instructions,updated_at:new Date().toISOString()}}
function render(row){$('sumDate').textContent=row.battle_date||'—';$('sumTime').textContent=txt(row.battle_time_utc).slice(0,5)+' UTC';$('sumName').textContent=row.event_name||'Not specified';const t=row.timeline||{};$('sumPhase').textContent=t.phaseTwoMinute==null?'Not set':'Minute '+t.phaseTwoMinute;$('sumW1').textContent=t.workshopOneMinute==null?'Not set':'Minute '+t.workshopOneMinute;$('sumW2').textContent=t.workshopTwoMinute==null?'Not set':'Minute '+t.workshopTwoMinute;$('savedAt').textContent=row.updated_at?new Date(row.updated_at).toLocaleString():'—';const target=$('summaryNotes');target.replaceChildren();const i=row.instructions||{};for(const k of fields){if(!i[k])continue;const article=document.createElement('article'),heading=document.createElement('h3'),p=document.createElement('p');heading.textContent=labels[k];p.textContent=i[k];article.append(heading,p);target.append(article)}}
async function save(){if(busy)return;try{const payload=collect();busy=true;$('save').disabled=true;status('Saving TAL Setup…');const {data:{user},error:authErr}=await sb.auth.getUser();if(authErr||!user)throw Error('Sign in to NEXA before saving TAL Setup.');payload.updated_by=user.id;const {data,error}=await sb.from('tal_setups').upsert(payload,{onConflict:'state_number'}).select('*').single();if(error)throw error;original=data;fill(data);render(data);show(false);status('TAL Setup saved for State '+state+'.')}catch(e){console.error('[NEXA TAL Setup save]',e);status('Could not save: '+(e.message||String(e)),true)}finally{busy=false;$('save').disabled=false}}
function startEdit(){if(!original)return;fill(original);show(true);$('cancel').hidden=false;status('Editing TAL Setup. Save when ready.');}
async function boot(){if(!Number.isInteger(state)||state<=0){status('Select an active NEXA State from Home before opening TAL Setup.',true);return;}$('stateLabel').textContent='State '+state;try{if(!window.supabase?.createClient)throw Error('Supabase library failed to load.');sb=window.supabase.createClient(SB_URL,SB_KEY);const {data:{user},error:authErr}=await sb.auth.getUser();if(authErr||!user)throw Error('Sign in to NEXA first.');const {data,error}=await sb.from('tal_setups').select('*').eq('state_number',state).maybeSingle();if(error)throw error;original=data||null;fill(data);if(data){render(data);show(false);status('TAL Setup loaded from NEXA.')}else{show(true);$('cancel').hidden=true;status('No TAL Setup saved for this State yet.')}}catch(e){console.error('[NEXA TAL Setup boot]',e);status('TAL Setup unavailable: '+(e.message||String(e)),true);show(false)}}
$('save').addEventListener('click',save);$('edit').addEventListener('click',startEdit);$('cancel').addEventListener('click',()=>{if(!original)return;fill(original);render(original);show(false);status('Changes discarded; showing saved TAL Setup.')});document.querySelectorAll('#editor input,#editor textarea').forEach(el=>el.addEventListener('input',()=>{dirty=true;status('Unsaved changes — press Save TAL Setup.')}));window.addEventListener('beforeunload',e=>{if(!dirty)return;e.preventDefault();e.returnValue=''});boot();
})();
</script>
</body>
</html>
