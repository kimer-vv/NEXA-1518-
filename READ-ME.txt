Replace ONLY the root file ui-i18n-final.js.

Root cause fixed in v58.9:
index.html declares `const supabaseClient`, which is a global lexical binding and is NOT exposed as window.supabaseClient.
v58.8 checked window.supabaseClient, so it could not see the same client that WOS Accounts was already using.

Expected: Kimer in center, Test in orbit, Add Account as an orbital object, single tap = Passport, double tap = active account switch.
